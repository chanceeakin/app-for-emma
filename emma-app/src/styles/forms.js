import colors from './colors'
import type Styles from './../types/Styles.js.flow'

export const forms: Styles = {
  input: {
    height: 40,
    borderColor: 'gray',
    borderBottomWidth: 1,
    color: colors.darkBlue,
    minWidth: 200
  }
}

export default forms
